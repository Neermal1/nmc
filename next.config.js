/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['admin.nmcth.edu'], // Add your external domain here
  },
}

module.exports = nextConfig
